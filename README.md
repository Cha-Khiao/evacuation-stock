# 📦 EvacStock — ระบบจัดการคลังสิ่งของศูนย์อพยพ

> ระบบบริหารจัดการสต็อกสิ่งของบรรเทาทุกข์ สำหรับศูนย์อพยพ รองรับการทำงานแบบหลายศูนย์ (Multi-Shelter) พร้อมระบบการอนุมัติ และติดตามประวัติแบบเรียลไทม์

---

## 🗂️ สารบัญ

- [ภาพรวมระบบ](#ภาพรวมระบบ)
- [Tech Stack](#tech-stack)
- [โครงสร้างโปรเจกต์](#โครงสร้างโปรเจกต์)
- [ฟีเจอร์หลัก](#ฟีเจอร์หลัก)
- [ระบบสิทธิ์การใช้งาน (Role)](#ระบบสิทธิ์การใช้งาน-role)
- [การติดตั้ง (Installation)](#การติดตั้ง-installation)
- [Environment Variables](#environment-variables)
- [การใช้งานครั้งแรก (First Setup)](#การใช้งานครั้งแรก-first-setup)
- [API Reference](#api-reference)
- [Data Models](#data-models)
- [Workflow การทำงาน](#workflow-การทำงาน)

---

## ภาพรวมระบบ

EvacStock ถูกพัฒนาขึ้นเพื่อแก้ปัญหาการจัดการสิ่งของบรรเทาทุกข์ในสถานการณ์ฉุกเฉิน โดยแยกการจัดการออกเป็น **คลังส่วนกลาง** (บริหารโดย Admin) และ **คลังประจำศูนย์อพยพ** (บริหารโดย Staff) พร้อมระบบขอเบิกสิ่งของที่ต้องผ่านการอนุมัติก่อนโอนของ

```
คลังส่วนกลาง (Admin)
        │
        │  Staff ส่งคำขอเบิก
        ▼
  [ระบบอนุมัติ] ──❌ ปฏิเสธ → คืนสต็อกกลับส่วนกลาง
        │
        │ ✅ อนุมัติ
        ▼
คลังประจำศูนย์อพยพ (Staff)
        │
        │  แจกจ่ายหน้างาน
        ▼
  ผู้ประสบภัย / ประชาชน
```

---

## Tech Stack

| ส่วน | เทคโนโลยี |
|------|-----------|
| Frontend | Next.js 14 (App Router), React, TypeScript |
| UI Framework | Bootstrap 5 |
| Charts | Chart.js + react-chartjs-2 |
| Authentication | NextAuth.js (JWT Strategy) |
| Database | MongoDB + Mongoose ODM |
| File Handling | SheetJS (xlsx) |
| Notifications | react-hot-toast, SweetAlert2 |
| Theme | next-themes (Light / Dark Mode) |
| Font | Google Fonts — Kanit |

---

## โครงสร้างโปรเจกต์

```
src/
├── app/
│   ├── api/
│   │   ├── auth/[...nextauth]/   # NextAuth Handler
│   │   ├── items/                # API สต็อกสิ่งของ
│   │   ├── transactions/         # API เบิกจ่าย / รับเข้า
│   │   ├── requests/             # API ระบบขอเบิกและอนุมัติ
│   │   ├── shelters/             # API ข้อมูลศูนย์อพยพ
│   │   ├── users/                # API บัญชีผู้ใช้งาน
│   │   └── setup/                # API สร้าง Admin คนแรก
│   ├── dashboard/                # หน้าแผงควบคุม
│   ├── items/                    # หน้าจัดการสต็อก
│   ├── transactions/             # หน้าเบิกจ่ายสิ่งของ
│   ├── requests/                 # หน้าระบบขอเบิก
│   ├── history/                  # หน้าประวัติการทำรายการ
│   ├── shelters/                 # หน้าจัดการศูนย์อพยพ
│   ├── users/                    # หน้าจัดการบัญชีผู้ใช้
│   └── login/                    # หน้าเข้าสู่ระบบ
├── components/
│   ├── Sidebar.tsx               # แถบเมนูด้านซ้าย
│   ├── Header.tsx                # แถบหัวหน้าจอ
│   └── Pagination.tsx            # Component แบ่งหน้า
├── lib/
│   └── mongodb.ts                # MongoDB Connection Helper
├── models/
│   ├── Item.ts                   # โมเดลสิ่งของ
│   ├── Transaction.ts            # โมเดลประวัติรับเข้า/จ่ายออก
│   ├── Request.ts                # โมเดลคำขอเบิก
│   ├── Shelter.ts                # โมเดลศูนย์อพยพ
│   └── User.ts                   # โมเดลผู้ใช้งาน
├── types/
│   └── next-auth.d.ts            # Type definitions สำหรับ Session
└── middleware.ts                 # Route Protection Middleware
```

---

## ฟีเจอร์หลัก

### 🏠 Dashboard
- แสดงสรุปจำนวนสต็อก, รายการใกล้หมด, รายการหมด
- กราฟโดนัทแสดงสัดส่วนสิ่งของแยกหมวดหมู่
- กราฟแท่งเปรียบเทียบรับเข้า vs เบิกจ่าย
- ตารางแจ้งเตือนสต็อกต่ำกว่า 50 ชิ้น
- แสดงประวัติความเคลื่อนไหว 5 รายการล่าสุด

### 📥 รับเข้าสิ่งของ (Items)
- เพิ่มสิ่งของทีละรายการผ่านฟอร์ม
- นำเข้าข้อมูลแบบกลุ่มผ่านไฟล์ Excel (รองรับ .xlsx, .xls)
- ค้นหาและกรองสต็อกตามหมวดหมู่
- ส่งออกรายงานสต็อกเป็นไฟล์ Excel

### 📤 เบิกจ่ายสิ่งของ (Transactions)
- Admin: จ่ายของลงศูนย์อพยพที่เลือก
- Staff: แจกจ่ายให้ผู้ประสบภัยหน้างาน พร้อมระบุชื่อผู้รับ
- รองรับการเบิกจ่ายแบบกลุ่มผ่านไฟล์ Excel
- ตรวจสอบสต็อกก่อนยืนยัน พร้อม Popup แสดงรายการที่มีปัญหา

### 🛎️ ระบบขอเบิก / อนุมัติ (Requests)
- Staff ส่งคำขอเบิกสิ่งของจากคลังส่วนกลาง (กรอกมือหรืออัปโหลด Excel)
- ระบบจอง (Reserve) สต็อกทันทีที่ส่งคำขอ เพื่อป้องกันการเบิกซ้ำ
- Admin อนุมัติ → ของโอนเข้าคลังศูนย์อัตโนมัติ
- Admin ปฏิเสธ → ของคืนกลับคลังส่วนกลางอัตโนมัติ

### 🗂️ ประวัติการทำรายการ (History)
- แสดงประวัติ IN/OUT แยกตาม Role (Admin เห็นของส่วนกลาง, Staff เห็นของศูนย์ตัวเอง)
- ค้นหาตามชื่อสิ่งของ, ผู้ทำรายการ, ศูนย์อพยพ
- กรองตามประเภทรายการ (IN/OUT)
- ส่งออกประวัติทั้งหมดเป็นไฟล์ Excel

### 🏕️ จัดการศูนย์อพยพ (Shelters) — Admin เท่านั้น
- นำเข้าข้อมูลศูนย์จากไฟล์ Excel หรือ JSON
- กรองตามประเภท (ศูนย์พักพิงหลัก / บ้านญาติ) และสถานะความจุ
- ส่งออกรายชื่อศูนย์ทั้งหมดเป็น Excel

### 👥 จัดการบัญชีผู้ใช้ (Users) — Admin เท่านั้น
- สร้างบัญชี Admin / Staff
- ผูก Staff เข้ากับศูนย์อพยพประจำ
- ลบบัญชีผู้ใช้งาน

---

## ระบบสิทธิ์การใช้งาน (Role)

| ฟีเจอร์ | ADMIN | STAFF |
|---------|:-----:|:-----:|
| Dashboard | ✅ | ✅ |
| จัดการศูนย์อพยพ | ✅ | ❌ |
| จัดการบัญชีผู้ใช้ | ✅ | ❌ |
| รับเข้าสิ่งของ (คลังส่วนกลาง) | ✅ | ❌ |
| รับเข้าสิ่งของ (คลังศูนย์ตัวเอง) | ❌ | ✅ |
| เบิกจ่ายออกสู่ศูนย์อพยพ | ✅ | ❌ |
| แจกจ่ายให้ผู้ประสบภัยหน้างาน | ❌ | ✅ |
| ส่งคำขอเบิกจากส่วนกลาง | ❌ | ✅ |
| อนุมัติ / ปฏิเสธคำขอเบิก | ✅ | ❌ |
| ดูประวัติ (คลังส่วนกลาง) | ✅ | ❌ |
| ดูประวัติ (คลังศูนย์ตัวเอง) | ❌ | ✅ |

---

## การติดตั้ง (Installation)

### ข้อกำหนด

- Node.js >= 18
- MongoDB (Atlas Cloud หรือ Local)

### ขั้นตอน

```bash
# 1. Clone โปรเจกต์
git clone https://github.com/your-username/evacstock.git
cd evacstock

# 2. ติดตั้ง Dependencies
npm install

# 3. ตั้งค่า Environment Variables
cp .env.example .env.local
# แก้ไขค่าใน .env.local ตามขั้นตอนด้านล่าง

# 4. รันในโหมด Development
npm run dev

# 5. Build สำหรับ Production
npm run build
npm start
```

---

## Environment Variables

สร้างไฟล์ `.env.local` ที่ root ของโปรเจกต์:

```env
# MongoDB Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>

# NextAuth Secret (สุ่มค่าใดก็ได้ที่ยาวและซับซ้อน)
NEXTAUTH_SECRET=your_super_secret_key_here

# URL ของแอป (ใช้ http://localhost:3000 ในโหมด Dev)
NEXTAUTH_URL=http://localhost:3000
```

---

## การใช้งานครั้งแรก (First Setup)

หลังจากรันแอปแล้ว ให้เข้าถึง URL ต่อไปนี้ **เพียงครั้งเดียว** เพื่อสร้างบัญชี Admin คนแรก:

```
GET http://localhost:3000/api/setup
```

ระบบจะสร้างบัญชีผู้ดูแลระบบด้วยข้อมูลดังนี้:

| | |
|---|---|
| **Email** | `admin@evac.com` |
| **Password** | `admin1234` |

> ⚠️ **คำเตือน:** เปลี่ยนรหัสผ่านหลังจากเข้าระบบครั้งแรกทันที และ API นี้จะถูกปิดกั้นอัตโนมัติเมื่อมีบัญชีในระบบแล้ว

---

## API Reference

### Items `/api/items`

| Method | Description | Query / Body |
|--------|-------------|-------------|
| `GET` | ดึงรายการสต็อก | `?email=` (กรองตาม Role) |
| `POST` | รับเข้าสต็อก (รายการเดี่ยวหรืออาร์เรย์) | `{ name, category, stockQuantity, email }` |

### Transactions `/api/transactions`

| Method | Description | Query / Body |
|--------|-------------|-------------|
| `GET` | ดึงประวัติการทำรายการ | `?email=` (กรองตาม Role) |
| `POST` | บันทึกการเบิกจ่าย (ลดสต็อก) | `[{ itemName, quantity, destinationShelterId, actionBy, note, email }]` |

### Requests `/api/requests`

| Method | Description | Body |
|--------|-------------|------|
| `GET` | ดึงคำขอเบิกทั้งหมด | - |
| `POST` | ส่งคำขอเบิก (Staff) | `{ email, requestedBy, items, note }` |
| `PUT` | อนุมัติ / ปฏิเสธ (Admin) | `{ requestId, status, actionBy, rejectReason? }` |

### Shelters `/api/shelters`

| Method | Description | Body |
|--------|-------------|------|
| `GET` | ดึงข้อมูลศูนย์อพยพทั้งหมด | - |
| `POST` | สร้างศูนย์อพยพ (รายการเดี่ยวหรืออาร์เรย์) | `{ name, district, shelterType, ... }` |

### Users `/api/users`

| Method | Description | Query / Body |
|--------|-------------|-------------|
| `GET` | ดึงรายชื่อผู้ใช้ทั้งหมด | - |
| `POST` | สร้างบัญชีผู้ใช้ใหม่ | `{ name, email, password, role, shelterId? }` |
| `DELETE` | ลบบัญชีผู้ใช้ | `?id=` |

---

## Data Models

### Item
```typescript
{
  name: string           // ชื่อสิ่งของ
  category: string       // หมวดหมู่ (อาหาร, น้ำดื่ม, ยา, ฯลฯ)
  stockQuantity: number  // จำนวนคงเหลือ
  shelterId: ObjectId | null  // null = คลังส่วนกลาง
}
```

### Transaction
```typescript
{
  itemId: ObjectId              // อ้างอิง Item
  type: 'IN' | 'OUT'           // รับเข้า / จ่ายออก
  quantity: number
  destinationShelterId?: ObjectId
  actionBy: string              // ชื่อผู้ทำรายการ
  note?: string
}
```

### Request
```typescript
{
  shelterId: ObjectId           // ศูนย์อพยพที่ขอ
  items: [{ itemId, itemName, quantity }]
  status: 'PENDING' | 'APPROVED' | 'REJECTED'
  requestedBy: string
  actionBy?: string             // Admin ที่พิจารณา
  note?: string
  rejectReason?: string
}
```

### User
```typescript
{
  name: string
  email: string                 // ใช้เป็น Username
  password: string              // Hashed ด้วย bcrypt
  role: 'ADMIN' | 'STAFF'
  shelterId?: ObjectId          // เฉพาะ STAFF
}
```

---

## Workflow การทำงาน

### การรับเข้าสต็อก
```
Staff/Admin กรอกฟอร์ม หรืออัปโหลด Excel
    → API ตรวจสอบ Role จาก email
    → เพิ่มสต็อกในคลังที่ถูกต้อง (ส่วนกลาง หรือ ศูนย์ตัวเอง)
    → บันทึก Transaction ประเภท IN
```

### การเบิกจ่ายสิ่งของ
```
เลือกสิ่งของ + จำนวน → ตรวจสอบสต็อกเพียงพอ
    → หักสต็อกออกจากคลัง
    → บันทึก Transaction ประเภท OUT
```

### การขอเบิกและอนุมัติ
```
Staff ส่งคำขอ → ระบบจองสต็อกส่วนกลางทันที (กัน Over-Booking)
    → Admin เห็นคำขอสถานะ PENDING
    ├── กด อนุมัติ → โอนของเข้าคลังศูนย์ + บันทึก OUT (ส่วนกลาง) + IN (ศูนย์)
    └── กด ปฏิเสธ → คืนสต็อกกลับส่วนกลาง + บันทึกเหตุผล
```

---

## รูปแบบไฟล์ Excel สำหรับนำเข้า

ระบบรองรับไฟล์ `.xlsx` / `.xls` ที่มีคอลัมน์อย่างน้อย 2 คอลัมน์:

| ชื่อสิ่งของ / name / item | จำนวน / qty / quantity | หมวดหมู่ / category (optional) |
|--------------------------|----------------------|-------------------------------|
| ข้าวสาร | 100 | อาหาร |
| น้ำดื่ม 1.5L | 200 | น้ำดื่ม |

> ระบบจะค้นหา Header Row อัตโนมัติในแถวแรกสุด 20 แถว รองรับทั้งชื่อคอลัมน์ภาษาไทยและอังกฤษ

---

## ผู้พัฒนา

พัฒนาเพื่อใช้งานในระบบบริหารจัดการศูนย์อพยพ &copy; 2026 Evacuation Center Management System